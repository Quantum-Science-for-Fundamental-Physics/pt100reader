name: Build Apps

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install py2app

      - name: Build .app with py2app
        run: |
          python setup.py py2app

      - name: Archive macOS .app
        run: |
          mkdir -p dist_archive
          cp -R dist/*.app dist_archive/
          cd dist_archive
          zip -r MyApp-mac.zip *.app

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyApp-mac
          path: dist_archive/MyApp-mac.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build Windows executable
        run: |
          pyinstaller --onefile --windowed my_program.py
          mkdir dist_archive
          copy dist\my_program.exe dist_archive\

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: MyApp-win
          path: dist_archive/my_program.exe
